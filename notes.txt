implement the following:
    > tutorial
    > random buying of items /
    > change item prices /
    > buying supplies /
    > possibly rotting of items
    > monthly paying of bills /
    > random market item prices
    > law of supply and demand formulas
    > SAVE GAME

Stuff:
    > define table_interface at game !!!!!!!!!!
    > change has_saved_game to:
        save_file = {
            name : data,
            name : data,
            name : data
        }

        def 
        if len(save_file) == 0:

    > put everything in tables